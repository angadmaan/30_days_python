# Can you change the values inside a list which is contained in setS?

s = {8, 7, 12, "Joe", [1,2]}

# We can't change the values inside a list which is contained in set.

# Because list is mutable and set is immutable.



